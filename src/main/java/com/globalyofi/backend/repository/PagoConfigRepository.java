package com.globalyofi.backend.repository;

import com.globalyofi.backend.entity.PagoConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PagoConfigRepository extends JpaRepository<PagoConfig, Long> {
}
